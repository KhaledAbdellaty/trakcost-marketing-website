services:
  trakcost-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trakcost-landing
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
