import { Link } from '@/i18n/routing';

// This is a wrapper around next-intl's Link if we need to add standard styles or logic
// But for now, we just export the configuration-aware Link
export { Link };
